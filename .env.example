# Netlify Database stellt NETLIFY_DB_URL automatisch beim Deploy bereit.
# Lokal über `netlify database` / `netlify dev` konfiguriert — hier nichts eintragen.

# Phase 4 — ML-Bewertungsservice (separater Dienst)
ML_API_URL=

# Phase 3 — transaktionale E-Mails (optional)
# RESEND_API_KEY=
